# repogithub_tom

Dieses Repository wird sowohl lokal (Terminal) als auch remote (code.claude.com) mit Claude Code verwendet.

## Workflow

- Remote-Änderungen werden über GitHub synchronisiert
- Lokal: `git pull` holt die neuesten Änderungen
- Remote: Änderungen werden automatisch gepusht
